.venv/
__pycache__/
*.pyc
.env
logs/

# User-specific skill configuration (copy from skills.example.json)
skills.json

# User-specific intent dispatcher configuration (copy from intents.example.json)
intents.json

# Pairing QR — contains a linking URI for your Signal account
qr.png

# Isolated Claude Code config dir for the bridge — holds the bridge's own
# stored credentials. Never commit.
.claude-config/
